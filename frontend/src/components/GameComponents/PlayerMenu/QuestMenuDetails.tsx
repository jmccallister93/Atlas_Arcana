import { PlayerInfo } from "../Interfaces";

interface QuestMenuDetailsProps {
    questItems?: any[];
    player: PlayerInfo;
    updatePlayerData: (updatedPlayer: PlayerInfo) => void;
  }

const QuestMenuDetails:React.FC = () => {
    return ( <></> );
}
 
export default QuestMenuDetails;